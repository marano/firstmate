--- /Users/marano/.no-mistakes/evidence/01M2KKZED2R2GVEVX8MN5GSEHM/base-demo-ship-nm-brief.md	2026-09-15 19:53:46
+++ /Users/marano/.no-mistakes/evidence/01M2KKZED2R2GVEVX8MN5GSEHM/fixed-demo-ship-nm-brief.md	2026-09-15 19:53:32
@@ -26,8 +26,11 @@
 1. Never push to the default branch. Never merge a PR.
 2. Stay inside this worktree; modify nothing outside it.
 3. Use gh-axi for GitHub operations and chrome-devtools-axi for browser operations.
+   One exception: gh-axi has no data-mode body read, so to get a PR body (or any stored text) back as data, read it
+   straight to a file with `gh api repos/<owner>/<repo>/pulls/<n> --jq .body`. Its `pr view --full` and `pr list --fields body`
+   output is rendered text - escaped and reflowed for reading - never a source to recover a body from by unescaping it.
 4. Report status by appending one line:
-   `echo "{state}: {one short line}" >> '/tmp/fm-brief-base.7rV3tm/home/state/demo-ship-nm.status'`
+   `echo "{state}: {one short line}" >> '/tmp/fm-brief-fixed.6nznol/state/demo-ship-nm.status'`
    States: working, needs-decision, blocked, paused, done, failed.
    Each append wakes firstmate, so report sparingly: only phase changes a supervisor
    would act on (setup done, bug reproduced, fix implemented, validation passed) and the
@@ -45,8 +48,8 @@
 5. If you hit the same obstacle twice, append `blocked: {why}` and stop; firstmate will help.
 6. If a decision belongs above the implementation worker (product choices, destructive actions),
    append `needs-decision: {summary of options}` and stop. Firstmate will reply with the decision.
-   For a no-mistakes ask-user gate specifically, escalate all ask-user findings as one event plus one snapshot file, using that same shape even when the gate holds only a single ask-user finding: write only the ask-user findings, verbatim and unparaphrased (id, severity, file, line, description, authority), to `/tmp/fm-brief-base.7rV3tm/home/data/demo-ship-nm/nm-<run>-findings.txt`, then report the gate with
-   `needs-decision [key=nm-<run>-<step>]: ask-user findings=<id1>,<id2>,... file=/tmp/fm-brief-base.7rV3tm/home/data/demo-ship-nm/nm-<run>-findings.txt`
+   For a no-mistakes ask-user gate specifically, escalate all ask-user findings as one event plus one snapshot file, using that same shape even when the gate holds only a single ask-user finding: write only the ask-user findings, verbatim and unparaphrased (id, severity, file, line, description, authority), to `/tmp/fm-brief-fixed.6nznol/data/demo-ship-nm/nm-<run>-findings.txt`, then report the gate with
+   `needs-decision [key=nm-<run>-<step>]: ask-user findings=<id1>,<id2>,... file=/tmp/fm-brief-fixed.6nznol/data/demo-ship-nm/nm-<run>-findings.txt`
    naming every ask-user finding id from that gate. The status line only points at the file; it never restates or summarizes a finding's content.
    A decision or blocker you opened stays open until a `resolved` line carrying its exact key lands; a later `done:` or `working:` line never closes it, even when the answer is what started that work.
    Firstmate's reply normally writes that closing line at answer time; when a blocker or wait clears WITHOUT a firstmate reply, append `resolved: {how it cleared}` yourself (same `[key=<slug>]` if you opened it with one) as you resume.
@@ -64,15 +67,15 @@
    timed-out call was only waiting for a read while the run kept working.
 
 # Firstmate instruction inbox
-Firstmate steers you through durable message files in '/tmp/fm-brief-base.7rV3tm/home/state/demo-ship-nm.inbox'.
-When a terminal message says an instruction is waiting there - and at any natural checkpoint when you are unsure - list '/tmp/fm-brief-base.7rV3tm/home/state/demo-ship-nm.inbox'/*.msg, read and act on each message in numeric order, then acknowledge each handled message by moving it: `mv '/tmp/fm-brief-base.7rV3tm/home/state/demo-ship-nm.inbox'/NNN.msg '/tmp/fm-brief-base.7rV3tm/home/state/demo-ship-nm.inbox'/handled/`.
+Firstmate steers you through durable message files in '/tmp/fm-brief-fixed.6nznol/state/demo-ship-nm.inbox'.
+When a terminal message says an instruction is waiting there - and at any natural checkpoint when you are unsure - list '/tmp/fm-brief-fixed.6nznol/state/demo-ship-nm.inbox'/*.msg, read and act on each message in numeric order, then acknowledge each handled message by moving it: `mv '/tmp/fm-brief-fixed.6nznol/state/demo-ship-nm.inbox'/NNN.msg '/tmp/fm-brief-fixed.6nznol/state/demo-ship-nm.inbox'/handled/`.
 The move IS the acknowledgement: without it firstmate rings again and eventually treats you as stuck. An empty or absent inbox needs no action.
 
 # Project memory
-If `AGENTS.md` or `CLAUDE.md` already exists, or if this task produced durable project-intrinsic knowledge, run `/tmp/fm-brief-base.7rV3tm/bin/fm-ensure-agents-md.sh .` in the worktree.
+If `AGENTS.md` or `CLAUDE.md` already exists, or if this task produced durable project-intrinsic knowledge, run `/Users/marano/.no-mistakes/worktrees/2d7095a62a33/01M2KKZED2R2GVEVX8MN5GSEHM/bin/fm-ensure-agents-md.sh .` in the worktree.
 Record only project knowledge useful to almost every future session.
 For anything the codebase already shows, prefer a pointer to the authoritative file, command, or doc over copying the detail.
-If you touch a project `AGENTS.md`, follow `/tmp/fm-brief-base.7rV3tm/bin/fm-ensure-agents-md.sh`'s self-governance contract in the same pass.
+If you touch a project `AGENTS.md`, follow `/Users/marano/.no-mistakes/worktrees/2d7095a62a33/01M2KKZED2R2GVEVX8MN5GSEHM/bin/fm-ensure-agents-md.sh`'s self-governance contract in the same pass.
 Keep it proportionate: skip `AGENTS.md` edits for trivial tasks that produced no durable project knowledge.
 
 # Definition of done
